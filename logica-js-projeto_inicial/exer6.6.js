// Solicitar um número ao usuário
let numero = prompt("Digite um número para iniciar a contagem progressiva:");
let contador = 0;

 // Executa o loop while até atingir o número inserido
alert ("Contagem Progressiva:")
 while (contador <= numero) {
   alert(contador);
   contador++;
 }
alert ("Contagem progressiva concluída!")